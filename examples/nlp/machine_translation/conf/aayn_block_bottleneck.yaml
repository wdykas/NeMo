defaults:
  - aayn_bottleneck

name: BlockBottleneck

model:
  max_num_blocks: 16  # seq is divided by this number at each hiearchy level
  num_hierar_levels: 2

exp_manager:
  name: AAYNBlockBottleneck
